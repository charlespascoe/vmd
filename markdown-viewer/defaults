[styles]
  heading_base = clear,bold,fgcolour(208)
  heading_index_base = fgcolour(160)
  heading3 = faint
  heading4 = faint
  heading5 = faint
  heading6 = faint
  strong = bold
  emphasis = italic
  inline_code = clear,fgcolour(196),bgcolour(52)
  link = underline,fgcolour(82)
  link_index = fgcolour(82)
  link_hint = fgcolour(240)
  list_bullet = fgcolour(208)

